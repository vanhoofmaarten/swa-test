{
  "name": "",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "npm run build --workspaces",
    "watch": "npm run watch --workspaces",
    "prestart": "npm run build",
    "start": "swa start packages/app --api-location packages/api",
    "test": "echo \"No tests yet...\""
  },
  "devDependencies": {
    "@azure/functions": "^3.0.0",
    "@types/node": "^17.0.35",
    "typescript": "^4.8.0-dev.20220524"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "node-fetch": "^3.2.4"
  }
}
